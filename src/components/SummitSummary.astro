---
interface Props {
	content: {
		heading: string;
		description: string;
	};
}

const { content } = Astro.props;
---

<section class="summit-summary">
	<div class="summary-container">
		<div class="vertical-text">EVENT<br/>HIGHLIGHTS</div>
		<div class="content-column">
			<div class="bento-grid">
				<!-- Row 1-3, Col 1: Photo (1x3) -->
				<div class="bento-item bento-photo bento-1x3">
					<img src="/images/event-photos/_DSC5343.png" alt="Cloud Summit attendees networking during the event" />
				</div>

				<!-- Row 1-2, Col 2-4: Green Charity (3x2) -->
				<div class="bento-item bento-green bento-3x2">
					<h3>$10,000+</h3>
					<p>Raised for Charity to date</p>
				</div>

				<!-- Row 1-2, Col 5-6: Photo (2x2) -->
				<div class="bento-item bento-photo bento-2x2">
					<img src="/images/event-photos/_DSC5450.png" alt="Audience listening to a Cloud Summit speaker session" />
				</div>

				<!-- Row 3, Col 2: Yellow (1x1) -->
				<div class="bento-item bento-yellow"></div>

				<!-- Row 3-5, Col 3-4: Dark Center Cloud Summit (2x3) -->
				<div class="bento-item bento-dark bento-2x3">
					<img src="/images/cloud-summit-stack-logo.svg" alt="Cloud Summit" class="bento-logo" />
				</div>

				<!-- Row 3-4, Col 5-6: Blue Attendees (2x2) -->
				<div class="bento-item bento-blue bento-2x2">
					<h3>500+</h3>
					<p>In-Person Attendees</p>
				</div>

				<!-- Row 4-5, Col 1-2: Pink Speakers (2x2) -->
				<div class="bento-item bento-pink bento-2x2">
					<h3>19</h3>
					<p>Speakers</p>
				</div>

				<!-- Row 5-7, Col 5-6: Photo (2x3) -->
				<div class="bento-item bento-photo bento-2x3">
					<img src="/images/event-photos/_DSC5457.png" alt="Cloud Summit community members connecting the stage" />
				</div>

				<!-- Row 6-7, Col 1-2: Photo (2x2) -->
				<div class="bento-item bento-photo bento-2x2">
					<img src="/images/event-photos/_DSC5559.png" alt="Live cloud talk in progress at Cloud Summit" />
				</div>

				<!-- Row 6-7, Col 3-4: Red Sponsors (2x2) -->
				<div class="bento-item bento-red bento-2x2">
					<h3>12+</h3>
					<p>Sponsors & Communities</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.summit-summary {
		background: black;
		color: white;
		padding: 3rem 1.5rem;
		position: relative;
		overflow: hidden;
	}

	.summary-container {
		max-width: 1440px;
		margin: 0 auto;
		padding: 0 1.5rem;
		position: relative;
	}

	.vertical-text {
		font-family: "Doto", ui-monospace, monospace;
		font-size: 2.5rem;
		font-weight: 500;
		color: #ffffff;
		opacity: 1;
		writing-mode: horizontal-tb;
		transform: none;
		white-space: nowrap;
		letter-spacing: 0.14em;
		line-height: 0.95;
		margin-bottom: 2rem;
		text-transform: uppercase;
		-webkit-font-smoothing: antialiased;
	}

	.content-column {
		width: 100%;
	}

	.bento-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1rem;
		margin-top: 2rem;
	}


	.bento-item {
		background: rgba(61, 120, 171, 0.2);
		border: 1px solid rgba(61, 120, 171, 0.3);
		border-radius: 1rem;
		padding: 1.5rem;
		transition: transform 0.2s, background 0.2s;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		text-align: center;
		min-height: 100px;
	}

	.bento-item.bento-photo {
		padding: 0;
	}

	.bento-photo:hover {
		background: rgba(61, 120, 171, 0.5);
		transform: translateY(-4px);
	}

	.bento-item h3 {
		font-family: "Squada One", system-ui, sans-serif;
		font-size: 2rem;
		margin: 0 0 0.5rem 0;
		color: white;
		line-height: 1;
	}

	.bento-item p {
		margin: 0;
		font-family:
			"Noto Sans",
			system-ui,
			-apple-system,
			BlinkMacSystemFont,
			"Segoe UI",
			Roboto,
			Oxygen,
			Ubuntu,
			Cantarell,
			sans-serif;
		font-size: 1rem;
		color: white;
		line-height: 1.3;
	}

	.bento-logo {
		max-width: 120px;
		height: auto;
		margin-bottom: 1rem;
	}

	.bento-date {
		font-size: 1.25rem;
		margin-bottom: 0.25rem;
	}

	/* Color variants */
	.bento-green {
		background: rgb(168, 251, 90);
	}

	.bento-green:hover {
		background: rgb(168, 251, 90);
	}

	.bento-green h3,
	.bento-green p {
		color: black;
	}

	.bento-yellow {
		background: rgb(255, 193, 77);
	}

	.bento-yellow:hover {
		background: rgb(255, 193, 77);
	}

	.bento-dark {
		background: rgb(45, 45, 45);
	}

	.bento-dark:hover {
		background: rgb(45, 45, 45);
	}

	.bento-blue {
		background: rgb(61, 120, 171);
	}

	.bento-blue:hover {
		background: rgb(61, 120, 171);
	}

	.bento-pink {
		background: rgb(232, 121, 249);
	}

	.bento-pink:hover {
		background: rgb(232, 121, 249);
	}

	.bento-red {
		background: rgb(251, 113, 133);
	}

	.bento-red:hover {
		background: rgb(251, 113, 133);
	}

	.bento-photo {
		background: rgba(61, 120, 171, 0.4);
		border: 2px solid rgba(61, 120, 171, 0.3);
		padding: 0;
		overflow: hidden;
	}

	.bento-photo img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	/* Span classes */
	.bento-1x3 {
		grid-column: span 1;
		grid-row: span 3;
	}

	.bento-3x2 {
		grid-column: span 3;
		grid-row: span 2;
	}

	.bento-2x2 {
		grid-column: span 2;
		grid-row: span 2;
	}

	.bento-2x3 {
		grid-column: span 2;
		grid-row: span 3;
	}

	/* Mobile: force a clean 2-column grid (no big spans) */
	@media (max-width: 768px) {
		.bento-item p {
			font-size: 0.9rem;
		}

		.bento-grid {
			grid-template-columns: 60% 40%;
			grid-auto-flow: row dense;
			grid-auto-rows: minmax(120px, auto);
			gap: 0.75rem;
		}

		.bento-1x3,
		.bento-3x2,
		.bento-2x2,
		.bento-2x3 {
			grid-column: span 1;
			grid-row: span 1;
		}

		/* Keep photos in the 60% column, cards in the 40% column */
		.bento-item {
			grid-column: 2;
		}

		.bento-item.bento-photo {
			grid-column: 1;
		}

		/* Put the Cloud Summit logo card in the 60% column */
		.bento-item.bento-dark {
			grid-column: 1;
		}

		.bento-item {
			min-height: 120px;
		}

		.bento-item.bento-photo {
			aspect-ratio: 4 / 3;
		}

		.bento-photo img {
			width: 100%;
			height: 100%;
			object-fit: cover;
		}

		/* Title on top, bento grid below */
		.summary-container {
			display: flex;
			flex-direction: column;
			align-items: stretch;
			gap: 0;
			padding: 0 1rem;
		}

		.vertical-text {
			writing-mode: horizontal-tb;
			transform: none;
			margin-bottom: 1.25rem;
			width: 100%;
			text-align: center;
			font-size: clamp(1.5rem, 5vw, 2.25rem);
			letter-spacing: 0.14em;
			line-height: 1.05;
			padding: 0;
			white-space: normal;
		}

		.content-column {
			width: 100%;
			min-width: 0;
		}

		.bento-grid {
			margin-top: 0;
		}
	}

	@media (min-width: 769px) {
		.summit-summary {
			padding: 4.5rem 2rem;
		}

		.summary-container {
			padding: 0 2rem;
			display: block;
		}

		.vertical-text {
			font-family: "Doto", ui-monospace, monospace;
			font-size: 6rem;
			font-weight: 500;
			writing-mode: vertical-rl;
			transform: rotate(180deg);
			position: absolute;
			left: -10rem;
			bottom: 0;
			margin-bottom: 0;
			letter-spacing: 0.14em;
			opacity: 1;
			color: #ffffff;
		}

		.content-column {
			width: 100%;
			position: relative;
		}

		.bento-grid {
			grid-template-columns: repeat(6, 1fr);
			margin-top: 0;
		}

		.bento-item {
			padding: 2rem;
			min-height: 150px;
		}

		.bento-item.bento-photo {
			padding: 0;
		}

		.bento-item h3 {
			font-size: 3rem;
		}

		.bento-item p {
			font-size: 1.125rem;
		}

		.bento-logo {
			max-width: 180px;
		}

		.bento-date {
			font-size: 1.5rem;
		}
	}
</style>
